# 排行榜视觉素材

这里存放排行榜页面的视觉验收截图和参考图，方便后续改 UI 时对照。

这些文件不是线上运行时静态资源。线上页面用到的 CSS/JS 仍在 `frontend/leaderboard/static/`，包内数据资源仍在 `assets/card_catalog.json`。

## screenshots

- `leaderboard-visual-vps.png`：VPS 桌面页面截图。
- `leaderboard-card-scale-2x.png`：卡片缩放调整参考图。
- `leaderboard-mobile-optimized.png`：移动端优化参考图。
- `leaderboard-mobile-check.png`：移动端验收截图。
- `leaderboard-mobile-after-deploy.png`：部署后移动端截图。
- `local-archetype-leaderboard.png`：本地流派榜截图。
- `local-archetype-reference-style.png`：本地流派样式参考图。
