GitHub提交描述规范
1. 当用户要求 commit、提交、push、pull request、PR、开 PR、合并请求时，默认使用中文补充描述。
2. commit message、PR title、PR body、变更摘要、验证说明都优先使用中文。
3. 如果使用 Conventional Commits，类型前缀可保留英文，例如 `feat`、`fix`，但后面的说明写中文。
4. PR 描述至少包含：变更内容、原因背景、验证方式。
5. 如果未运行测试，需要在 PR 或提交说明中用中文注明原因。
6. 除非用户明确要求英文，否则不要生成纯英文提交说明或 PR 描述。
